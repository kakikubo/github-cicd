name: Artifacts
on: workflow_dispatch
permissions: {}
jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - run: date > date.txt              # アーティファクトの生成
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7  # アーティファクトのアップロード
        with:
          name: uploaded                  # アーティファクト名
          path: date.txt                  # アーティファクト対象のパス
          retention-days: 7               # アーティファクトの保存期間
  download:
    runs-on: ubuntu-latest
    needs: [upload]                       # uploadジョブの実行を待つ
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8 # アーティファクトのダウンロード
        with:
          name: uploaded                  # アーティファクト名
          path: downloaded                # ダウンロード先のパス
      - run: cat downloaded/date.txt      # アーティファクトを標準出力
