name: Hello
on: push
jobs:
  hello:
   runs-on: ubuntu-latest
   steps:
     - run: |
        invalid-command
        tree -a
        echo "Hello, World!"
     - uses: actions/checkout@v4
       with:
          ref: main
